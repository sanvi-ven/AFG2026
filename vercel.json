{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable && export PATH=\"$PWD/flutter/bin:$PATH\" && flutter config --enable-web && cd frontend && flutter pub get && flutter build web --dart-define=API_BASE_URL=${API_BASE_URL:-https://api.yourdomain.com}",
  "outputDirectory": "frontend/build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "API_BASE_URL": {
      "description": "Base URL for backend API",
      "example": "https://your-backend-api.com"
    }
  }
}
